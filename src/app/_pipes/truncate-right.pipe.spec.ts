import { TruncateRightPipe } from './truncate-right.pipe';

describe('TruncateRightPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateRightPipe();
    expect(pipe).toBeTruthy();
  });
});
