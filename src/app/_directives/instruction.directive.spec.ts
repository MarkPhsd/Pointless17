import { InstructionDirective } from './instruction.directive';

describe('InstructionDirective', () => {
  it('should create an instance', () => {
    const directive = new InstructionDirective();
    expect(directive).toBeTruthy();
  });
});
