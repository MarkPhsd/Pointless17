import { NgControlAttributeDirective } from './ng-control-attribute.directive';

describe('NgControlAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgControlAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
