import { NumericDirectiveDirective } from './numeric-directive.directive';

describe('NumericDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
