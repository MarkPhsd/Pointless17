import { UserAuthorizedDirective } from './user-authorized.directive';

describe('UserAuthorizedDirective', () => {
  it('should create an instance', () => {
    const directive = new UserAuthorizedDirective();
    expect(directive).toBeTruthy();
  });
});
