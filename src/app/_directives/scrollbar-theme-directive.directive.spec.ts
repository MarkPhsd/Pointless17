import { ScrollbarThemeDirectiveDirective } from './scrollbar-theme-directive.directive';

describe('ScrollbarThemeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollbarThemeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
