@page {
  size: auto;
  margin: 0mm 05mm 15mm 05mm;
}

@media print {
  .address {
    font-size: 18pt;
    font-weight: 600;
    align-items: center;
    position: relative;
    text-align: center;
  }

  .company-name {
    font-size: 13pt;
    font-weight: 600;
    align-items: center;
    position: relative;
    text-align: center;
  }

  .itemName,
  .itemprice,
  .description,
  .quantity,
  .amountPaid,
  .total,
  .name,
  .method {
    font-size: 13pt;
    font-weight: 600;
  }

  .kitchenFont {
    font-size: 18pt;
    font-weight: 600;
    align-items: right;
    position: relative;
    text-align: right;
  }

  .kitchenItem {
    font-size: 18pt;
    font-weight: 600;
    align-items: right;
    position: relative;
    text-align: right;
  }

  .header-info {
    font-size: 14pt;
    font-weight: 600;
    align-items: left;
    text-align: left;
  }

  .total,
  .totals,
  .amountPaid {
    font-size: 13pt;
    font-weight: 600;
    align-items: right;
    position: relative;
    text-align: right;
  }

  .itemprice,
  .item-price {
    font-size: 13pt;
    font-weight: 600;
    align-items: right;
    position: relative;
    text-align: right;
  }

  .right-text {
    font-size: 13pt;
    font-weight: 600;
    align-items: right;
    position: relative;
    text-align: right;
  }

  .company-name {
    font-size: 1.2em;
    font-weight: 600;
    align-items: center;
    position: relative;
    text-align: center;
  }

  .address,
  .companyname {
    font-size: 0.8em;
    align-items: center;
    position: relative;
    text-align: center;
  }

  .itemName,
  .itemprice,
  .quantity {
    align-items: center;
    font-size: 0.8em;
  }

  .amountPaid,
  .total,
  .item-price {
    font-size: 0.8em;
    text-align: right;
  }

  .name,
  .method {
    font-size: 0.8em;
  }

  .description {
    font-size: 0.8em;
  }

  .grid-receipt {
    display: grid;
    grid-template-columns: 50px auto 80px;
    grid-template-rows: auto;
  }

  .grid-totals {
    display: grid;
    grid-template-columns: 120px auto auto;
    gap: 10px;
    float: right;
    margin-top: 20px;
  }

  .spacing {
    margin-top: 20px;
  }

  .items-span-columns-2 {
    grid-column-end: span 2;
  }

  .items-span-columns-3 {
    grid-column-end: span 3;
  }

  .grid-totals-58.signature-line.spacing-58 {
    margin-top: 50px !important;
    margin-bottom: 30px !important;
  }
}
